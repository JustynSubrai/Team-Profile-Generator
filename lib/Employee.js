class Empolyee {
    constructor(name, id, email, role) {
        this.name = name,
        this.id = id,
        this.email = email,
        this.role = role
    }

    getName() {
        console.log({
            name: this.name
        });
    };

    // getId() {

    // };

    // getEmail() {

    // };
// Returns 'Employee'
    // getRole() {

    // };
};